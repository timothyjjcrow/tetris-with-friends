{
  "buildCommand": "cd packages/types && npm run build && cd ../client && npm run build",
  "outputDirectory": "packages/client/dist",
  "installCommand": "npm install && cd packages/types && npm run build",
  "framework": "vite"
}
