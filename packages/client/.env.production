# Production environment variables
# Primary server (update with your Railway URL when deployed)
VITE_SERVER_URL=https://tetris-with-friends-server-production.up.railway.app 

# Fallback server (when primary is down)
VITE_FALLBACK_SERVER_URL=https://tetris-with-friends-api.onrender.com 